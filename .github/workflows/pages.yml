name: Deploy demo to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      # --omit=optional 금지: rolldown 네이티브 바인딩이 optional이라 빌드가 깨진다
      - run: npm ci
      - run: npm test

      # 공개 명세(PROTOCOL.md)가 여전히 참인가 — 문서만 보고 짠 클라이언트로
      # 진짜 룸에 들어가 본다. 남이 이 문서로 클라이언트를 만들고 있으므로
      # 조용히 어긋나면 우리가 아니라 그쪽이 막힌다.
      - run: npm run smoke:protocol

      # 부팅 스모크 = 배포 게이트. 첫 방문자 동선을 실제 브라우저로 밟아 보고,
      # 실패하면 여기서 멈춰 깨진 데모가 올라가지 않게 한다.
      # 스모크는 base 없이 빌드한 산출물로 돌린다 (프리뷰가 "/" 로 서빙되도록).
      - run: npx playwright install --with-deps chromium
      - run: npm run build -w client
        env:
          VITE_DEMO: "1"
      - run: npm run smoke:boot

      - run: npm run build -w client
        env:
          VITE_DEMO: "1"
          VITE_BASE: /${{ github.event.repository.name }}/
      - uses: actions/upload-pages-artifact@v5
        with:
          path: client/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
