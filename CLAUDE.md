# CLAUDE.md — 이 저장소에서 일하는 규칙

기와장터(GIWA Village)는 **GIWA 체인 위의 한옥 저잣거리 3D 소셜 플랫폼**이다.
지갑이 아바타이고, 거래가 노점이고, 신원 인증이 상인 뱃지다.

이 파일은 사람과 AI 에이전트가 함께 읽는 정답지다. 세션이 바뀌어도 같은 결정을
내리게 하려고 둔다. 새 기능을 넣기 전에 여기 있는 규칙과 먼저 대조할 것.

---

## 1. 절대 규칙

1. **에셋은 원장에 적힌 것만 반입한다.** 이미지·오디오·폰트를 쓸 수 있게 됐지만,
   조건이 있다 — `data/assets.json`에 출처·저작자·라이선스를 적고 `npm run assets`로
   받는다. 허용 라이선스는 **CC0 · CC-BY · OFL** 셋뿐이고, 원장에 없는 바이너리가
   들어오면 `npm test`가 실패한다(용량 예산 20MB도 함께 본다).
   - **다른 게임·상용 에셋은 조금 고쳐도 반입 금지.** 변형해도 2차적저작물이다.
     참고할 것은 파일이 아니라 *왜 그렇게 들리는가*(어택 길이·배음·레이어·리버브)다.
   - **바이너리는 원본 그대로 둔다.** 손질(게인·피치·필터·타일링·색조)은 코드에서
     한다. 그래야 "정말 그 출처에서 온 것인가"에 체크섬으로 답할 수 있다.
   - **여전히 코드로 만드는 것:** 한옥·아바타·도깨비 지오메트리와 **배경음 전체**.
     절차 생성이 이기는 자리(끝나지 않는 음악·무한 변주)는 파일로 바꾸지 않는다.
     받는 것은 합성이 약한 자리뿐이다 — 발소리·동전처럼 한 번 나고 마는 실물 소리,
     사진이 아니면 안 되는 표면 질감, 손으로 그릴 수 없는 글자꼴.
   - `client/src`에는 여전히 바이너리를 두지 않는다. 에셋은 `client/public` 아래.
   *예외:* `media/`는 우리가 직접 찍은 시연 캡처다.
2. **게임 플레이 보상은 양도 불가(소울바운드).** 칭호·전리품·장신구는 전송할 수
   없다. 거래되는 것은 재화(코스메틱·쿠폰)뿐이고, 금융상품·오더북은 만들지 않는다.
   포지셔닝은 게임이 아니라 **아바타 소셜 플랫폼**이다.
3. **스케일·성능 작업은 실서비스 전까지 미룬다.** 인덱서·읽기 캐시·구역 샤딩 같은
   것은 [기술 문서 §7](https://hakhyun-kim.github.io/giwa-village/tech.html#scale)에
   설계만 남기고 구현하지 않는다. 지금 병목은 트래픽이 아니라 아직 없는 사용자다.
   **유일한 예외: 그 변경이 코드를 줄일 때.** 제안이 들어오면 먼저 되묻는다 —
   *이걸로 코드가 줄어드는가?* 빨라지지만 복잡해지는 것은 미룬다.
4. **부정적 어감의 명칭을 쓰지 않는다.** (실례: `[홍등]` → `[등불]`로 전면 교체함)

---

## 2. 세계관 어휘 (표기 통일)

| 것 | 부르는 이름 | 컨트랙트 |
|---|---|---|
| 개인 상점 | **노점** (저잣거리에 편다) | GiwaMarketV3 |
| 값 흥정 | **흥정** (에스크로로 즉시 체결) | GiwaOffers |
| 모임 | **길드**, 그 원정이 **던전** | GiwaGuilds |
| 주간 보스 | **도깨비 토벌** | GiwaBoss |
| 모닥불에 함께 앉기 | **온기** | GiwaHearth |
| 토요일 21시(KST) 2배 창 | **장날** | GiwaHearth |
| 유저 제작 문양 | **문양 공방** | GiwaWorkshop |
| 소울바운드 칭호 | **칭호** (이름표에 붙는다) | GiwaHonors |
| 온보딩 | **촌장의 부탁** (무료 첫걸음 뒤 3가지 길 선택) | — (클라이언트) |

마을 시각은 **KST 기준**이다(주야 사이클·장날 모두). 시간을 다루는 코드를 고칠 때
UTC로 슬쩍 바꾸지 말 것 — 장날이 조용히 어긋난다.

---

## 3. 아키텍처

```
client/  Vite + React Three Fiber + zustand + viem + Colyseus.js
  chain/    컨트랙트별 읽기·쓰기 (모든 쓰기는 wallet.ts의 queueTx 하나를 통과한다)
  config/   주소·ABI·상수
  game/     3D 씬 (절차 생성 한옥·아바타·주야 사이클) + feel.ts(타격감)
            + collide.ts(배치표·충돌 — 그리는 쪽과 막는 쪽이 같은 표를 읽는다)
  ui/       HUD·다이얼로그 + Welcome.tsx(첫 방문자에게 시연 여부를 한 번 묻는 카드)
            + QuestLog.tsx(촌장의 부탁 — 시연 중에는 비켜 준다)
  demo/     서버 없는 모드(VITE_DEMO=1): NPC 로컬 시뮬 + 온체인은 실동작
            (걸음 규칙은 wander.ts — 순수 모듈이라 npm test가 그대로 걸려 본다)
            showcase.ts는 두 갈래 — 실거래 시연과 쓰기 0인 구경 모드
  audio/    배경음(파일 0개 · 절차 생성) + 효과음(합성 + 반입한 CC0 조각)
            audio.ts가 믹서·리버브·귀, samples.ts가 반입 조각과 폴백
  dev/      자동화 훅 (?rafshim, window.__giwa)
server/  Colyseus 룸 — 선택 사항이다. 서버 0으로도 마을이 돈다.
contracts/ Solidity 10종 (전부 GIWA Sepolia 배포·검증 완료)
scripts/ 테스트·배포·봇·스모크
sdk/     외부에서 프로필을 읽는 패키지
guide/   사람이 읽는 문서 (놀기·이어붙이기·검증·이어만들기) — README는 색인이다
```

**문서는 README에 쌓지 않는다.** 루트 `README.md`는 랜딩과 문서 지도이고, 주제별
본문은 `guide/` 아래 한 파일씩이다(PLAY·FEATURES·WALLET·TESTING·AGENTS·ROADMAP·
DEPLOY). 새 내용은 해당 주제 파일에 넣고, README에는 링크 한 줄만 는다.
`PROTOCOL.md`·`ASSETS.md`는 루트에 남긴다 — 둘 다 `npm test`가 경로로 읽는다.

**클라이언트도 하나가 아니다.** 마을은 구현체가 아니라 프로토콜이다 —
루트의 `PROTOCOL.md`가 공개 명세이고, 배치표·충돌·소리 임계는
`client/public/world.json`으로 구워 누구나 읽는다. **표를 옮기지 말고 굽는다:**
원본은 여전히 §4의 TypeScript 한 곳이고 world.json은 생성물이다. 배치·상수를
고쳤으면 `npm run export-world`를 함께 돌린다(잊으면 `npm test`가 잡는다).
서버 메시지를 추가·변경했으면 `PROTOCOL.md`도 함께 고친다 — 이것도 `npm test`가
서버 소스와 대조하고, `npm run smoke:protocol`이 **문서만 보고 짠 클라이언트로**
실제 룸에 들어가 본다(colyseus.js를 일부러 안 쓴다 — 쓰면 아무것도 증명 못 한다).

**핵심 결정 셋:**

- **체인이 곧 서버다.** 노점 레지스트리·길드·던전 정산·프레즌스가 전부 온체인이라
  서버 없이 멀티플레이가 된다. 서버(Colyseus)는 부드러운 15Hz 동기화를 위한
  **가속기**일 뿐, 없으면 없는 대로 돈다. 새 기능은 이 순서로 묻는다 —
  *체인만으로 되는가? 안 되면 왜?*
- **모든 지갑 쓰기는 `queueTx`로 직렬화한다.** 같은 지갑에서 병렬 전송하면 nonce가
  충돌한다("replacement transaction underpriced"). 효과음·tx 진행 칩도 여기 하나에
  달려 있다. 새 쓰기를 추가하면 반드시 이 큐를 통과시킬 것.
- **프레즌스 비컨은 `queueTx(fn, true)`** — 사람이 누른 전송이 아니므로 소리를
  내지 않는다. 자동으로 나가는 전송을 새로 만들면 똑같이 silent로.

**게임필(`game/feel.ts`)의 규율 셋:**

1. **매 프레임 바뀌는 값은 zustand에 넣지 않는다.** 흔들림 한 번에 씬 전체가
   수십 번 리렌더된다. 평범한 변수로 두고 `useFrame`이 읽어 간다.
2. **새 메시·파티클·라이트를 만들지 않는다.** 이미 있는 카메라·재질·라이트의
   숫자를 몇 프레임 흔드는 것으로 끝낸다 — 저사양에서도 비용이 사실상 0이다.
3. **즉각 반응은 화면이, 사실 확인은 체인이 한다.** R을 누른 그 프레임에
   섬광·흔들림·효과음이 나가고, *얼마나 깎였는지*는 영수증이 확정한 뒤
   숫자로 뜬다. 남이 때린 것도 함께 뜬다 — 코업이 코업으로 보여야 한다.
   흔들림은 `prefers-reduced-motion`이면 끈다(피드백 자체는 남긴다).

**소리(`audio/`)의 규율 다섯:**

1. **원시 도구는 둘뿐이다** — `audio.ts`의 `tone()`(음)과 `noise()`(잡음). 배경음의
   북도 효과음의 타격도 전부 이 둘의 조합이다. 두 파일에 같은 버퍼 코드를 복사하지
   말 것. 성공은 올라가고 실패는 내려간다. 연달아 날 수 있는 소리(타격)는
   레이트 리밋(45~70ms) — 연타가 기관총이 되면 안 된다.
2. **배경음은 한 곡이 아니라 상태다.** 도깨비 곁에 서면 북이 붙은 토벌 트랙으로
   갈아타고, 깎일수록 빨라진다. 무엇을 언제 놓을지는 스텝 시퀀서가, 예약은
   **룩어헤드 스케줄러**(오디오 시계 위 0.5초 앞)가 한다 — `setTimeout`으로 음을
   하나씩 울리면 숨은 탭에서 스로틀돼 박자가 무너진다.
3. **소리와 화면의 임계는 하나다.** 심장박동이 도는 순간과 이름표가 붉게 맥동하는
   순간은 같아야 하므로 임계는 `audio/track.ts` 한 곳에 있고 그리는 쪽이 그 함수를
   부른다. 트랙 표는 Web Audio를 모르는 순수 모듈이라 `npm test`가 그대로 돌린다 —
   소리는 귀로만 확인되므로, 규칙을 코드 밖에 두면 아무도 지키지 않는다.
4. **모든 소리는 한 믹서를 지난다.** 배경음·효과음이 각자 스피커로 직행하면 겹칠 때
   찢어진다. `audio.ts`가 세우는 버스 둘(music·sfx) → 컴프레서 하나 → 스피커이고,
   같은 자리에서 **마당 울림(리버브)**을 태운다. 리버브가 없으면 아무리 잘 만든
   소리도 "스피커에서 나는 소리"로 들리고, 있으면 같은 공간에서 나는 소리가 된다.
   자리가 있는 소리(모닥불)는 `playLoopAt`으로 3D에 놓고, 귀는 카메라가 아니라
   **아바타**에 둔다(`syncListener` — Player가 매 프레임 부른다).
5. **반입한 소리는 있으면 쓰고 없으면 합성음으로 돌아간다.** `samples.ts`의 재생
   함수는 실패하면 `false`를 돌려주고 부르는 쪽이 폴백한다 — 404·오프라인·디코드
   실패 어느 쪽이든 마을은 조용해지지 않는다. 대신 조용히 나빠지는 것을 막으려고
   `npm test`가 "가리키는 파일이 실제로 있는가"를 본다.

**첫 방문자 동선의 규율 셋:**

1. **딱 한 번만 묻는다.** 들어오면 촌장이 "대신 걸어서 보여줄까?"를 한 번 묻고
   (`ui/Welcome.tsx`), 답은 `giwa-welcome`에 남아 다시 묻지 않는다. 첫 방문 판정은
   `giwa-welcome`과 `giwa-quest-step`이 **둘 다 없을 때**다 — 온보딩을 이미 시작한
   사람에게 처음인 척 묻지 않기 위해서.
2. **빈손이어도 보여준다.** 처음 온 사람의 지갑은 대개 비어 있다. 잔액이 없으면
   쓰기를 하나도 하지 않는 **구경 모드**로 내려가 걸어서 보여주고, 포셋 링크는
   자막으로만 안내한다. 포셋 앞에 세워 두는 것이 가장 나쁜 첫인상이다.
   오래 기다리는 것은 녹화용 `?showcase=1` 갈래(`patient`)뿐이다.
3. **안내는 겹치지 않는다.** 시연 중에는 촌장의 부탁이 숨고(`store.showcasing`),
   시연이 끝나거나 ESC로 멈추면 다시 나와 다음 할 일을 잇는다.
   셋 다 `npm run smoke:boot`이 빈 localStorage로 검사한다 — 이 동선은 처음 온
   사람에게만 보이므로, 게이트가 없으면 조용히 망가진다.

---

## 4. 밸런스 수치는 어디 있는가

한 곳에 모으는 것이 원칙이지만, 컨트랙트(Solidity)와 클라이언트(TS)와 서버(TS)는
파일을 합칠 수 없다. 그래서 **어긋나면 테스트가 깨지도록** 묶어 뒀다.

| 수치 | 사는 곳 | 지키는 장치 |
|---|---|---|
| 던전 문 확률 (safe/bonus/trap) | `contracts/GiwaGuilds.sol` · `server/src/guilds.ts` · `client/src/chain/guilds.ts` | `npm test` — 세 곳의 경계값 일치 + 손익분기가 2~8층 안에 있을 것 |
| 흥정 하한선 판단 | `scripts/lib/haggle.mjs` **한 곳** (봇과 테스트가 공유) | `npm test` |
| 장날 판정 | `contracts/GiwaHearth.sol` · `client/src/chain/hearth.ts` | `npm run test:local` — 표본 64개 대조 |
| 주야 사이클 밝기 | `client/src/game/daylight.ts` | `npm test` — 밤에도 읽을 수 있는 하한 |
| 마을 배치·충돌 크기 | `client/src/game/collide.ts` **한 곳** (Village.tsx가 이 표를 그린다) | `npm test` — 벽 통과 금지 + 모닥불·도깨비·포털 도달 가능 · `npm run smoke:boot` — 실프레임 |
| 주민 걸음 (속도·몸 크기·배회 반경) | `client/src/demo/wander.ts` **한 곳** | `npm test` — 진짜 노점 배치 위에서 1분 산책: 한 틱에 한 걸음 넘지 않기 · 벽 안에서 끝나는 틱 0 · `npm run smoke:boot` — 실브라우저 속도 표본 |
| 풍류 트랙·도깨비 빈사 임계 | `client/src/audio/track.ts` **한 곳** (Village.tsx가 붉은 맥동에 이 함수를 쓴다) | `npm test` — 표본 41개로 소리·화면 임계 대조 · `npm run smoke:boot` — 실브라우저에서 예약되는 오디오 노드 수 |
| 위 표들을 남이 읽는 형태로 구운 것 | `client/public/world.json` — **생성물이다** (`npm run export-world`) | `npm test` — 굽는 것을 잊으면 실패 + world.json만 읽고 걸어도 웹과 1mm 안에서 같은 벽에 막힐 것 |

수치를 고치면 **반드시 짝을 이루는 곳을 함께 고친다.** 봇·서버는 통과하는데
체인에서만 다르게 판정되는 것이 가장 잡기 어려운 종류의 버그다.

---

## 5. 검증 — 가스를 아끼는 순서

```bash
npm test                     # 로직 59건 · 체인 없음 · 1초 미만 (저장할 때마다)
npm run export-world         # 배치표를 client/public/world.json 으로 굽는다
npm run test:local           # 컨트랙트 10종 E2E 49건 · 로컬 anvil · 가스 0
npm run smoke:boot           # 실브라우저 부팅 게이트 (배포 전, CI에서도 돎)
npm run smoke:protocol       # PROTOCOL.md만 보고 짠 클라이언트로 룸에 입장 (서버 자동 기동)
npm run test:chain -- --yes  # 실배포본·Dojang·UP.ID 읽기 확인
npm run market-smoke         # 실거래 — 꼭 필요할 때만 (가스 든다)
```

- `test:local`은 anvil을 **chain-id 91342**로 띄워 체인 가드를 그대로 통과시킨다.
  없으면 설치 방법만 안내하고 종료한다(의존성에 넣지 않는 이유는 README 참고).
  **시간 여행이 핵심** — 장날·쿨다운·모닥불 창을 `evm_increaseTime`으로 점프해 본다.
- `npm run assets`는 반입 에셋을 출처에서 다시 받아 지문을 대조한다(`data/assets.json`
  이 원장, `ASSETS.md`는 그 생성물). 새 에셋은 원장에 먼저 적고 이 명령으로 받는다.
- `smoke:boot`은 `VITE_DEMO=1`로 빌드한 산출물을 실제 크롬으로 연다. **빈
  localStorage(첫 방문자)로** 여는 것이 요점이다 — 개발자 브라우저에는 온보딩
  진행 기록이 남아 있어 "첫 방문자에게만 나는 문제"를 영영 못 잡는다.
  공개 테스트넷 RPC에서 온 콘솔 에러는 일부러 걸러낸다. 남의 사정으로 게이트가
  빨간불이 되면 곧 아무도 안 본다.

### 자동화 훅

| 훅 | 쓰임 |
|---|---|
| `?rafshim` | 숨은 탭에서 rAF가 스로틀돼 헤드리스 검증이 멈추는 것을 막는다 (30Hz 타이머) |
| `?debug` | 프로덕션 빌드에서도 `window.__giwa` 노출 (개발 서버는 항상 노출) |
| `window.__giwa` | `state()` `pos()` `teleport(x,z)` `ready()` `bossHit(n)` — 타격 연출만 흉내(가스 0) · `walls()` `blocked(x,z)` — 충돌 검사 · `mood(트랙, 남은체력)` — 도깨비를 실제로 깎지 않고 배경음 트랙만 갈아 끼운다 · `remotes()` — 주민·타 플레이어 좌표(걸음이 튀는지는 시간에 따라 찍어야 보인다) |
| `?showcase=1` | 무조작 자동 시연 (시각을 정오로 고정 — 영상 보호) |
| `?slot=A` | 버너 지갑 슬롯으로 입장 (로컬 개발) |

---

## 6. 환경 함정 (실제로 겪은 것)

- **Git Bash에서 `VITE_BASE=/x/` 금지** — MSYS가 경로로 바꿔 망가뜨린다.
  PowerShell에서: `$env:VITE_BASE="/giwa-village/"; npm run build -w client`
- **`--omit=optional` 금지** — rolldown 네이티브 바인딩이 빠져 배포 빌드가 깨진다.
- **solc-js + optimizer 조합이 WASM 크래시** — optimizer를 끄고 컴파일한다.
  익스플로러 검증도 optimizer off 기준(standard-input, v0.8.36).
- **공개 RPC는 리플리카 지연이 있다** — 전송 직후 상태 조회가 한 박자 늦는다.
  잔액 비교 대신 **영수증 status + 이벤트**로 검증하고, 조회는 재시도 루프로.
- **`.hud`는 `pointer-events: none`** — HUD 버튼마다 `auto`를 되살려야 클릭이
  캔버스로 새지 않는다. `npm test`가 이걸 검사한다.

---

## 7. 다음 후보

우선순위 순. 착수 전에 3-Gate로 거른다 —
**① 코드로 만들 수 있는가 ② 결정(선택)을 더하는가 ③ 봇이 측정할 수 있는가.**

- [ ] **일일 봇 실주행을 콘텐츠로** — 아래 §8 설계 참고 (로컬 무가스 주행)
- [ ] **기록 카드 PNG** — 오늘 산 것·칭호·길드 층수를 캔버스로 그려 공유
- [ ] Dojang 어테스테이션 테스트넷 발급 협의
- [ ] 실브랜드 기프티콘 API 연동 조사
- [ ] RPC 읽기 캐시·인덱서·구역 샤딩 — **실사용자가 생긴 뒤에** (규칙 3)

---

## 8. 설계만 해 둔 것 — 일일 장터 기록 (미구현)

봇이 도는 것을 **콘텐츠로 승격**하는 패턴이다. 밸런스 봇은 원래 개발자만 보는
도구인데, 그 출력을 마을 안으로 들여보내면 매일 갱신되는 콘텐츠가 된다 —
백엔드 0인 채로.

**돌리는 곳은 로컬이다. 테스트넷이 아니다.**

```
anvil --chain-id 91342          # 로컬 체인 (가스 0, 시간 여행 가능)
  → 컨트랙트 10종 배포          # test-local.mjs가 이미 하는 일
  → (선택) 로컬 Colyseus 서버   # 붐비는 장터를 흉내 낼 때만
  → 상인 봇 N명 + 손님 봇       # 하루치를 몇 초로 점프해 돌린다
  → client/public/chronicle/<KST 날짜>.json  로 커밋
```

- **왜 로컬인가.** ① 포셋이 주소당 24시간 제한이라 테스트넷 주행은 매일을
  보장할 수 없다 ② GitHub Actions에 봇 개인키를 넣지 않아도 된다 ③ anvil은
  `evm_increaseTime`이 되므로 **하루치 장터를 몇 초에** 돌릴 수 있다.
  실거래 증명은 이미 `test:chain`·`market-smoke`가 따로 하고 있다.
- **비용이 드는 것은 하지 않는다.** 상시 서버 호스팅(Fly.io 등)도, CI에서의
  테스트넷 실거래도 넣지 않는다. 기록은 `npm run chronicle` 한 줄로 만들어
  커밋하고, 주기가 필요하면 **개발 머신의 작업 스케줄러**로 충분하다.
  (호스팅이 필요해지는 시점은 실사용자가 생긴 뒤다 — 규칙 3)
- **폴백이 기본값이다.** 마을은 기록 파일이 있으면 게시판으로 보여주고,
  없으면 게시판 자체를 숨긴다. 404가 나도 스모크가 실패하지 않도록
  `smoke-boot.mjs`의 에러 필터에 `chronicle/*.json`을 함께 넣을 것.
- **멱등성·시간대.** 오늘 파일이 있으면 즉시 종료한다. 날짜는 **KST 기준**으로
  짓고, 끝난 뒤 파일명과 기록 안의 날짜를 대조한다 — 이 검증이 없으면 자정
  근처에 하루 어긋난 기록이 조용히 커밋된다.
- **실패하면 침묵한다.** 봇이 교착하면 파일을 쓰지 않고 끝낸다. 폴백이 있으니
  **빈 데이터가 틀린 데이터보다 낫다.**
- **3-Gate 정직하게.** ① 절차성 ✅ ② 결정 추가 ❌ — 이건 결정을 늘리지 않는
  *콘텐츠*다. "다시 올 이유"로서 값어치가 있어 후보에 두지만, 코어 루프에
  선택을 더하는 항목보다 뒤다 ③ 검증가능 ✅ (기록 자체가 측정값)
