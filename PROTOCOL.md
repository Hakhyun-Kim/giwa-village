# 기와장터 프로토콜 — 누구든 클라이언트를 만들 수 있다

기와장터(GIWA Village)의 마을은 웹 클라이언트 하나가 아니다. **프로토콜이다.**
언리얼이든 고도든 순수 C든, 여기 적힌 것만 지키면 같은 마을에 서서 같은 사람들과
같은 벽에 막힌다.

이 문서는 그 약속이다. 구현체(`client/`)를 읽지 않아도 클라이언트를 만들 수 있게
쓴다 — 읽어야 알 수 있는 것이 남아 있다면 그건 이 문서의 버그다.

| | |
|---|---|
| 판(version) | **1** — `world.json`의 `version`과 같다 |
| 마을 데이터 | [`world.json`](client/public/world.json) · 라이브: `https://hakhyun-kim.github.io/giwa-village/world.json` |
| 굽는 명령 | `npm run export-world` (원본은 TypeScript, 이 파일은 그 생성물) |
| 검증 | `npm test` — 문서·데이터·서버가 어긋나면 빨간불 |
| 라이선스 | 프로토콜과 `world.json`은 MIT. 마음대로 구현해도 된다 |

---

## 0. 큰 그림 — 갈래가 둘이다

```
        ┌──────────────────────────────────────────┐
        │  당신의 클라이언트 (언리얼·고도·웹·봇)   │
        └───────────┬──────────────────┬───────────┘
                    │                  │
        ① 체인 경로 │ (필수·정본)      │ ② 룸 경로 (선택·가속기)
                    ▼                  ▼
         GIWA Sepolia 컨트랙트     Colyseus 룸 (15Hz)
         거래·길드·칭호·프레즌스   부드러운 위치 동기화
```

**① 체인이 정본이다.** 노점·거래·길드·던전 정산·칭호·프레즌스가 전부 온체인이다.
서버 없이도 마을이 돈다. 클라이언트가 이 갈래만 구현해도 완전한 시민이다.

**② 룸은 가속기다.** 15Hz 스냅샷으로 걸음이 부드러워지고, 서버 권위 던전이 열린다.
없으면 없는 대로 돈다 — 룸이 죽어도 마을은 안 죽는다. **거꾸로도 참이다: 룸이
말하는 것은 무엇도 돈이 아니다.** 잔액·소유권은 언제나 체인에서 다시 확인한다.

> 공개 데모(`https://hakhyun-kim.github.io/giwa-village/`)는 **① 만** 켜져 있다.
> 룸 서버는 상시 호스팅하지 않는다 — 붙어 보려면 저장소를 받아
> `npm run dev:server`로 `ws://localhost:2567`을 띄운다.

---

## 1. 마을 데이터 — `world.json`

한옥이 어디 서 있고 무엇이 길을 막는지는 이 파일 하나에 있다. 좌표를 손으로 옮겨
적지 말 것 — 이 파일을 읽어라. 원본이 바뀌면 이 파일이 바뀌고, 안 바뀌면 `npm test`가
깨진다.

### 1.1 좌표계

- 오른손 좌표계, **y가 위**. 지면은 `y = 0`. 평면 이동은 `x`/`z`만 쓴다.
- 단위는 미터. 마을 반경 **55**m — 그 밖으로는 못 나간다.
- 회전 `rot`은 라디안. 아바타는 **실제로 간 쪽**을 본다(가려던 쪽이 아니라).
  벽을 따라 미끄러지는데 얼굴만 벽을 향하면 게걸음처럼 보인다.
- 체인에 실을 때는 정수로 만든다 — `round(값 * 100)` (`chain.posScale`, **x100**).

### 1.2 무엇이 들어 있나

| 키 | 무엇 |
|---|---|
| `chain` | 체인 ID·RPC·익스플로러·컨트랙트 10종 주소와 `fromBlock`(이벤트 훑을 시작 블록) |
| `world` | 반경·아바타 반지름(0.42)·주민 반지름(0.35)/속도(2.4m·s⁻¹)/시뮬 주기(50ms)·포털/모닥불/도깨비 자리 |
| `layout` | 한옥 6채(위치·회전·벽 색)·나무 20그루·등불·간판 |
| `colliders` | **항상** 그 자리에 있는 것들의 충돌체 |
| `stallCollider` | 노점이 생겼을 때 세울 충돌체의 크기 |
| `audio` | 트랙 모양과 도깨비 빈사 임계 |

### 1.3 충돌 — 두 가지뿐이다

```jsonc
{ "kind": "circle", "x": 0, "z": 0, "r": 2.6 }                       // 분수·나무·등불
{ "kind": "box", "x": 12, "z": 8, "hw": 1.25, "hd": 0.16, "rot": 0 } // 한옥·간판·노점
```

`hw`/`hd`는 **반**너비/반깊이, `rot`은 y축 회전(라디안)이다.

움직인 **뒤에** 한 번 밀어낸다. 겹친 만큼만 밖으로 내보내므로 비스듬히 들어가면
저절로 미끄러진다 — 따로 미끄럼 계산을 하지 않는 이유다.

```
밀어내기(p, c, r):
  원이면:
    d ← |p - c|;  min ← c.r + r
    d ≥ min 이면 통과
    d < 0.0001 이면 p.x ← c.x + min  (한가운데 — 방향이 없으니 아무 쪽으로나)
    아니면 p ← c + (p - c)/d * min

  상자면:
    l ← (p - c)를 -c.rot 만큼 돌린 것        // 상자의 좌표계로
    ox ← c.hw + r - |l.x|;  oz ← c.hd + r - |l.z|
    ox ≤ 0 또는 oz ≤ 0 이면 통과
    얕게 파고든 축으로만 민다:
      ox < oz 이면 l.x ← sign(l.x) * (c.hw + r)
      아니면      l.z ← sign(l.z) * (c.hd + r)
    p ← c + l을 +c.rot 만큼 되돌린 것

한 틱:
  colliders 전부에 밀어내기(p, c, playerRadius)
  살아 있는 노점·도깨비 충돌체에도 같은 것
  |p| > world.radius 이면 p ← p / |p| * world.radius
```

순서가 중요하다 — 충돌체 배열 순서대로 돌려야 좁은 골목에서 같은 결과가 나온다.

> `npm test`가 이 의사코드를 그대로 옮겨 놓고 `world.json`만 읽어 마을 전체를
> 훑는다. 웹 클라이언트가 밀어낸 자리와 **1mm 안**에서 일치하지 않으면 실패한다
> (좌표를 0.1mm까지 반올림해 굽는 데서 오는 차이만 허용한다). 즉 위 규칙은
> 설명이 아니라 검사받는 명세다.

### 1.4 생겼다 사라지는 것

노점과 도깨비는 세계에 붙박이가 아니다. **보이는 동안만** 충돌체를 등록하고
사라질 때 지운다 — 없는 것에 부딪히는 것만큼 이상한 게 없다.

- 일반 노점: `stallCollider.normal` 크기, `rot = 0`
- 브랜드 점포(건물): `stallCollider.brand` 크기, `rot = z > 0 ? π : 0` (마당을 향한다)

---

## 2. 갈래 ① — 체인 경로 (필수)

정본이다. 주소는 `world.json`의 `chain.contracts`에서 읽는다(하드코딩하지 말 것 —
재배포되면 그 파일이 갱신된다). ABI는 `client/src/config/*.ts`에 있고, 읽기만 할
거라면 `sdk/`(viem 기반)가 `profileOf` 한 번으로 프로필·칭호·길드·노점을 준다.

### 2.1 프레즌스 — "누가 어디 있는가"

**GiwaPresence**는 이벤트만 내는 컨트랙트다. 상태를 저장하지 않는다.

```solidity
event Beacon(address indexed who, int32 x100, int32 z100,
             int16 vx100, int16 vz100, uint8 emote);

function beacon(int32 x100, int32 z100, int16 vx100, int16 vz100, uint8 emote);
```

- 좌표·속도는 **x100** 정수. 속도는 `(이번 위치 - 지난 위치) / 지난 뒤 흐른 초`.
- `emote`: `0` 없음 · `1` 👋 · `2` 🎁 · `3` 🛍️ · `4` 앉음(모닥불) · `255` **떠남**

**보내는 규칙** (그대로 따르길 권한다 — 체인이 조용해야 다 같이 쓴다):

| | 값 | 왜 |
|---|---|---|
| 최소 간격 | 2초 | 그보다 잦으면 nonce가 밀린다 |
| 움직임 문턱 | 0.05m | 제자리에서 떠는 비컨은 안 보낸다 |
| 하트비트 | 15초 | 안 움직여도 이만큼마다 한 번 (안 그러면 남의 화면에서 사라진다) |
| 잔액 하한 | 0.0003 ETH | 이보다 적으면 **관전 모드** — 읽기는 공짜다 |
| 나갈 때 | `emote = 255` | 유령이 남지 않는다 |

**읽는 규칙:**

- `getLogs`로 마지막으로 본 블록 다음부터 훑는다. 처음 붙을 땐 **60블록 뒤**부터.
- 2.5초마다. 창이 숨겨져 있으면 4번에 1번만(공개 RPC는 레이트리밋이 있다).
- **데드레커닝**: 마지막 비컨 위치 + 속도 × 흐른 시간, **최대 3초까지만** 예측한다.
- 60초 넘게 소식이 없는 피어는 지운다.

### 2.2 그 밖에 읽을 것

| 무엇 | 어디서 |
|---|---|
| 노점·매물·구매 | `market` — `event Purchased(address indexed buyer, address indexed seller, string itemId, uint256 amount, uint256 indexed purchaseId, uint256 tokenId)` |
| 프로필 한 번에 | `profile` — `profileOf(address)` (칭호·길드·노점·장신구) |
| 칭호 배지 | `honors` — 소울바운드. **전송 불가가 규칙이다** |
| 길드·던전 | `guilds` — 주차 blockhash 시드 |
| 모닥불·장날 | `hearth` — 장날은 **토요일 21시 KST** |
| 도깨비 | `boss` — 주간, `strike` 쿨다운 30초 |

⚠️ **모든 시각은 KST 기준이다.** 장날·주야 사이클 모두. UTC로 계산하면 조용히
어긋난다.

⚠️ **한 지갑에서 트랜잭션을 병렬로 보내지 말 것.** nonce가 충돌한다
(`replacement transaction underpriced`). 웹 클라이언트는 모든 쓰기를 큐 하나로
직렬화한다. 당신의 클라이언트도 그래야 한다.

---

## 3. 갈래 ② — 룸 경로 (선택)

[Colyseus](https://colyseus.io) 0.16 룸이다. **공식 C++/언리얼 SDK는 없지만, 이
서버는 붙이기 쉽다** — 이유는 아래 3.3에 있다.

### 3.1 자리 얻기 (HTTP)

```
POST http://<host>:2567/matchmake/joinOrCreate/village
Content-Type: application/json

{ "name": "나그네", "address": "0x…", "color": 16755200 }
```

응답:

```jsonc
{
  "room": { "roomId": "AbC123", "processId": "xyz", "name": "village" },
  "sessionId": "SmT9k",          // 이번 접속에서의 내 id
  "reconnectionToken": "…",      // 있으면 보관 (없어도 된다)
  "protocol": "ws"
}
```

옵션 셋 다 없어도 된다. 이름을 안 주면 `guest-XXXX`가 붙고, 같은 이름이 이미 있으면
서버가 뒤에 숫자를 붙인다.

### 3.2 소켓 열기

```
ws://<host>:2567/<processId>/<roomId>?sessionId=<sessionId>
```

### 3.3 프레임 — 첫 바이트가 전부다

```
┌────────┬──────────────────┬────────────────────┐
│ opcode │ 메시지 이름       │ 페이로드            │
│ 1 byte │ msgpack str      │ msgpack 아무 값     │
└────────┴──────────────────┴────────────────────┘
```

| opcode | 뜻 | 방향 |
|---|---|---|
| `10` | JOIN_ROOM — 입장 확정 | 서버 → 나 |
| `11` | ERROR | 서버 → 나 |
| `12` | LEAVE_ROOM (페이로드 없음) | 양쪽 |
| `13` | **ROOM_DATA** — 실제 메시지 전부 | 양쪽 |

**핵심: 이 룸은 Colyseus의 스키마 상태 동기화를 쓰지 않는다.** 서버가
`setState()`를 한 번도 부르지 않아서 직렬화기가 `none`이고, `ROOM_STATE(14)` ·
`ROOM_STATE_PATCH(15)` 프레임이 **아예 오지 않는다.** 남의 언어로 Colyseus를
붙일 때 제일 어렵고 제일 잘 깨지는 부분(`@colyseus/schema`의 델타 디코더)이
통째로 필요 없다는 뜻이다. 필요한 것은 **표준 msgpack 리더 하나**뿐이다.

- opcode 뒤는 msgpack 값 **둘**이 연달아 온다: 이름(문자열), 그리고 페이로드.
  하나의 리더로 이어서 읽으면 된다.
- 서버는 msgpack **record 확장을 쓰지 않는다**(`useRecords: false`). 평범한
  map/array/scalar만 온다.
- 보낼 때도 평범한 msgpack map이면 된다.
- 페이로드 없는 메시지(`ping`)는 opcode + 이름까지만 보낸다.

**입장 확인을 반드시 돌려보낼 것 — 여기서 제일 많이 막힌다.**

`JOIN_ROOM(10)`은 msgpack이 아니다. 길이가 앞에 붙은 문자열 둘이다:

```
[10][길이][재접속토큰][길이]["none"]
```

이게 오면 **한 바이트 `[10]`을 그대로 돌려보낸다.** 그 확인을 받기 전까지 서버는
당신을 "입장 중"으로 보고 보낼 것을 **큐에 쌓아 둔다** — 소켓은 멀쩡히 열려 있고
`snapshot`이 하나도 안 오는 상태가 된다. 조용해서 원인을 찾기 어렵다.

### 3.4 내가 보내는 것

| 이름 | 페이로드 | 뜻 |
|---|---|---|
| `ping` | 없음 | 살아 있음. **5초마다** — 안 보내면 유휴로 정리된다 |
| `move` | `{x, z, rot}` | 내 위치. 좌표는 실수 그대로(x100 아님) |
| `emote` | `"👋"` (문자열, 8자 이하) | 머리 위 표시 |
| `gift` | `{to, amountEth, tx}` | **이미 체인에서 끝난** 선물을 피드에 알린다 |
| `stalls:get` | 없음 | 노점 목록 요청 |
| `stall:open` | `{title, items:[{name, emoji, priceEth}]}` | 내 자리에 노점을 편다 (매물 1~3개) |
| `stall:close` | 없음 | 접는다 |
| `stall:buy` | `{stallId, itemId, tx}` | **이미 체인에서 끝난** 구매를 알린다 |
| `guilds:get` | 없음 | 길드 목록 요청 |
| `guild:create` | `{name, emblem}` | 길드 창설 |
| `guild:join` | `{guildId}` | 가입 |
| `guild:leave` | 없음 | 탈퇴 |
| `dungeon:enter` | 없음 | 원정 시작 (길드원만) |
| `dungeon:pick` | `{door}` — 0·1·2 | 문을 고른다 |
| `dungeon:bank` | 없음 | 지금까지 오른 층을 확정한다 |

`gift`·`stall:buy`의 `tx`는 **표시용**이다. 서버는 형식만 보고(0x + 64 hex) 잔액을
믿지 않는다 — 돈은 체인에서만 움직인다.

### 3.5 서버가 보내는 것

| 이름 | 페이로드 | 언제 |
|---|---|---|
| `snapshot` | `[{id, name, address, color, x, z, rot}, …]` | **15Hz**, 전원 |
| `leave` | `"세션id"` | 누가 나감 |
| `emote` | `{id, emote}` | 누가 이모트 |
| `gift` | `{kind:"gift", from, fromName, to, toName, amountEth, tx, at}` | 선물 피드 |
| `stalls` | `[{id, ownerAddress, ownerName, title, tag?, x, z, items[], brand?, theme?, createdAt}, …]` | 목록 요청·변경 |
| `stall:sale` | `{kind:"sale", stallId, stallTitle, itemName, itemEmoji, priceEth, buyer, buyerName, buyerAddress, ownerName, tx, at}` | 누가 삼 |
| `guilds` | `[{id, name, emblem, members[], dungeon:{floor}}, …]` | 목록 요청·변경 |
| `guild:error` | `"사람이 읽을 한 줄"` | 거절 사유 |
| `dungeon:state` | `{guildId, guildName, emblem, epoch, seedBlock, seedHash, offchain, floor, tentative, attempt}` | 원정 시작 |
| `dungeon:result` | `{outcome:"safe"\|"bonus"\|"trap", door, tentative, floor, ended}` | 문을 연 결과 |
| `dungeon:banked` | `{floors, floor}` | 확정됨 |

`snapshot`에는 **나도 들어 있다.** 첫 스냅샷에서 내 `id`를 찾아 그 자리를
출발점으로 삼고, 그다음부터 내 항목은 무시한다(내 위치는 내가 안다).

### 3.6 서버가 강제하는 것

거절은 조용하다 — 형식이 틀린 메시지는 **답 없이 버려진다**. 로그를 보고 고칠 것.

| | |
|---|---|
| 정원 | **60명** |
| 좌표 | ±55로 잘린다 |
| 이름 | 글자·숫자·`_.-`·공백만, 16자 |
| 노점 제목 | 20자, `<`·`>` 제거 |
| 매물 | 1~3개 · 이름 16자 · 가격 `0 < p ≤ 1` ETH (소수점 18자리까지) |
| tx 해시 | `^0x[0-9a-fA-F]{64}$` |
| 유휴 정리 | 하트비트가 **120초** 없으면 끊는다 |

노점 id는 `s-` + 주소 앞 8자리다. **지갑 하나에 노점 하나** — 다시 열면 덮어쓴다.

---

## 4. 만들기 전에 알아 둘 것

**필요한 것:** WebSocket · HTTP · msgpack 리더 · JSON 리더. 체인까지 하려면
keccak256 · secp256k1 · RLP · ABI 인코딩(EIP-1559).

**참고 구현이 하나 있다** — [`scripts/protocol-smoke.mjs`](scripts/protocol-smoke.mjs).
이 문서에 적힌 것만으로 짠 클라이언트다(**colyseus.js를 일부러 안 쓴다** — 쓰면
아무것도 증명하지 못한다). 필요한 만큼의 msgpack까지 합쳐 한 파일이니, 다른
언어로 옮길 때 이것부터 보면 된다. `npm run smoke:protocol`로 실제 룸에 들여보내
본다 — **이게 실패하면 서버가 아니라 이 문서가 틀린 것이다.**

**단계로 나누면 첫 화면까지 멀지 않다:**

1. `world.json`을 받아 한옥과 나무를 세운다 — 서버도 지갑도 필요 없다.
2. §1.3의 밀어내기를 넣는다. 이 시점에 이미 "걸어 다닐 수 있는 마을"이다.
3. 룸에 붙어 `snapshot`으로 남을 그린다. 여기까지가 멀티플레이의 전부다.
4. 체인 읽기(프레즌스·프로필)를 붙인다. 서명이 없으니 아직 쉽다.
5. 마지막에 서명. 여기서부터 비용이 는다 — 급하지 않다면 미뤄도 마을은 돈다.

**하면 안 되는 것:**

- 게임 플레이 보상(칭호·전리품·장신구)을 **전송 가능하게 만들지 말 것.**
  소울바운드는 규제 안전선이지 취향이 아니다.
- 오더북·금융상품을 얹지 말 것. 거래되는 것은 재화(코스메틱·쿠폰)뿐이다.
- 룸이 말한 잔액·소유권을 믿지 말 것. 정본은 언제나 체인이다.

---

## 5. 판이 바뀔 때

- `world.json`의 필드는 **늘기만 한다.** 모르는 키는 무시하면 된다.
- 형식을 깨는 변경은 `version`을 올리고 이 문서 맨 위 표를 고친다.
- 룸 메시지가 늘거나 서버 상수가 바뀌면 `npm test`가 이 문서를 검사해 빨간불을
  낸다 — 문서가 서버보다 늦는 일은 구조적으로 막아 뒀다.

무엇이 막히면 이슈로 남겨 달라. **읽어야 알 수 있는 것이 남아 있다면 이 문서의
버그다.**
